{
  "name": "netvision-noc",
  "version": "0.1.0",
  "private": true,
  "description": "Enterprise Network Operations Center and CRM platform",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace apps/web",
    "api": "npm run dev --workspace apps/api",
    "build": "npm run build --workspaces"
  }
}
